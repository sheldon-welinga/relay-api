/**
 * ResolverGlobalArgs are argument variables that are used in our resolvers
 *
 * Any new Resolver variable must be defined here
 */
export enum ResolverGlobalArgs {
  where = 'where',
  input = 'input',
  id = 'id',
}

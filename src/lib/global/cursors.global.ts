/**
 * Cursors are same as entity names and should always start with UpperCase letters
 *
 * They are used to uniquesly identify nodes in the relay graphql
 *
 */
export enum CursorsGlobal {
  User = 'User',
  Country = 'Country',
  City = 'City',
}

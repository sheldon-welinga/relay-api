export enum OperatorAliasEnum {
  '_eq' = '_eq',
  '_neq' = '_neq',
  '_in' = '_in',
  '_nin' = '_nin',
  '_gt' = '_gt',
  '_lt' = '_lt',
  '_gte' = '_gte',
  '_lte' = '_lte',
  '_like' = '_like',
  '_nlike' = '_nlike',
  '_ilike' = '_ilike',
  '_nilike' = '_nilike',
  '_similar' = '_similar',
  '_nsimilar' = '_nsimilar',
  '_regex' = '_regex',
  '_iregex' = '_iregex',
  '_nregex' = '_nregex',
  '_niregex' = '_niregex',
}

export enum OperatorNameEnum {
  'equals' = 'equals',
  'not_equals' = 'not equals',
  'in' = 'in',
  'not_in' = 'not in',
  'greater_than' = '>',
  'less_than' = '<',
  'greater_than_equals' = '>=',
  'less_than_equals' = '<=',
  'like' = 'like',
  'not_like' = 'not like',
  'like_case_insensitive' = 'like (case-insensitive)',
  'not_like_case_insensitive' = 'not like (case-insensitive)',
  'tilde' = '~',
  'all_tilde' = '~*',
  'not_tilde' = '!~',
  'not_all_tilde' = '!~*',
}

export enum OperatorDefaultEnum {
  'double_percent' = '%%',
  'empty_array' = '[]',
}
